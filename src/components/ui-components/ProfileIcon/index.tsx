import React from "react";

interface Props {
  src: string;
}

/**
 * @memo Aboutページで写真を表示するためのアイコン
 */
const ProfileIcon: React.FC<Props> = ({ src }) => {
  return <>ProfileIcon</>;
};

export default ProfileIcon;

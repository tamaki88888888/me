import React from "react";

interface Props {
  href: string;
  src: string;
}

/**
 * @memo ロゴを表示するためのアイコン
 */
const LogoIcon: React.FC<Props> = ({ href, src }) => {
  return <>LogoIcon</>;
};

export default LogoIcon;

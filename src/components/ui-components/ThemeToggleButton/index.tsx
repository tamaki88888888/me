import React from "react";

interface Props {
  dark: any;
  lignt: any;
}
/**
 * @memo Themeカラーを切り替えるためのインターフェースを表現するコンポーネント
 */
const ThemeToggleButton: React.FC<Props> = (dark, light) => {
  return <></>;
};

export default ThemeToggleButton;
